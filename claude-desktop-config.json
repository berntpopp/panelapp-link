{
  "_comment": "Add this block to claude_desktop_config.json (mcpServers). Replace the path. The stdio entry point crawls both PanelApp regions and builds the local SQLite database on first start.",
  "mcpServers": {
    "panelapp-link": {
      "command": "uv",
      "args": [
        "--directory",
        "/absolute/path/to/panelapp-link",
        "run",
        "panelapp-link-mcp"
      ],
      "env": {
        "PANELAPP_LINK_DATA__DATA_DIR": "/absolute/path/to/panelapp-link/data"
      }
    }
  }
}
