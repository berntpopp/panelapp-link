{
  "_comment": "Add this block to claude_desktop_config.json (mcpServers). Replace the path. The stdio entry point is a live PanelApp API client with an in-memory cache; it needs no data directory or build step.",
  "mcpServers": {
    "panelapp-link": {
      "command": "uv",
      "args": [
        "--directory",
        "/absolute/path/to/panelapp-link",
        "run",
        "panelapp-link-mcp"
      ]
    }
  }
}
